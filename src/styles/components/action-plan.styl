&-action-plan {
  font-size: $smallCopy;
  padding: $spacing*2;
  line-height: 1.6;
  +above(l) {
    padding: 0;
    margin-top: $vspacing*16;
  }
  .u-btn {
    font-size: $smallCopy;
    margin-top: $vspacing*2;
  }
}
