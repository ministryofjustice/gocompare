&-cost {
  cf();
  &__scale {
    cf();
    span(1/3);
    margin-bottom: $vspacing;
    img {
      width: 100%;
      height: auto;
    }
    &__label {
      span(1/2);
      &:last-of-type {
        text-align: right;
      }
    }
  }
  &__info {
    clear:both;
  }
}
