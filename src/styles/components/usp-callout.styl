&-usp-callout {
  blockquote {
    padding: $vspacing $hspacing*2;
    background-color: $boxbg;
  }

  // If the component is preceded immediately by a headed-text component then
  // change margins to match it.
  .c-headed-text + & {
    margin-top: -($vspacing*4);
  }
}
